services:
  - type: web
    name: your-app-name  # Change this (e.g., "hajicali-app")
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    env: python
